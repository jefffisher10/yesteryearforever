*Always pull first! before editing...

// Editing on Windows (using git bash//
git pull
(edit files)
git add .
git commit -m "Improve archive generator"
git push

// Editing on the Pi //
cd /var/www/html
git pull
(edit files)
git add .
git commit -m "I updated these files"
git push

// Editing from your phone (SSH into Pi) //
cd /var/www/html
git pull
(edit file with nano)
git add .
git commit -m "Quick fix"
git push

// Editing on Github to pi //
(edit file on Github)
Click [Commit Changes...]
(add comment)
on pi: cd /var/www/html
git pull
